import 'package:flutter/material.dart';

class MentorPresensiReportScreen extends StatelessWidget {
  const MentorPresensiReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MentorPresensiReportScreen'),
      ),
      body: const Center(
        child: Text('MentorPresensiReportScreen - Coming Soon'),
      ),
    );
  }
}
