import 'package:flutter/material.dart';

class PresensiReportScreen extends StatelessWidget {
  const PresensiReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PresensiReportScreen'),
      ),
      body: const Center(
        child: Text('PresensiReportScreen - Coming Soon'),
      ),
    );
  }
}
